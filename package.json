{
  "name": "magento-vscode",
  "displayName": "Magento VSCode",
  "description": "VSCode Plugin for better Magento 2 development workflow.",
  "version": "0.0.6",
  "publisher": "MatseVH",
  "repository": {
    "url": "https://github.com/matse2005/magento-vscode",
    "type": "git"
  },
  "preview": true,
  "engines": {
    "vscode": "^1.109.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "magentoContainer",
          "title": "Magento",
          "icon": "media/logo.svg"
        }
      ]
    },
    "views": {
      "magentoContainer": [
        {
          "id": "magentoProjects",
          "name": "Projects"
        }
      ]
    },
    "commands": [
      {
        "command": "magento.createModule",
        "title": "Magento: Create Module"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "magento.refreshProjects",
          "when": "view == magentoProjects",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "magento.createModule",
          "when": "explorerResourceIsFolder && resourcePath =~ /.*[/\\\\]app[/\\\\]code$/",
          "group": "magento@1"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js",
    "magento:create": "node ./scripts/create.mjs",
    "magento:sync": "node ./scripts/sync.mjs",
    "magento:publish": "npm run magento:sync && npm run pretest && npm run test && vsce publish"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.3",
    "@types/node": "18.x",
    "@types/vscode": "^1.109.0",
    "@typescript-eslint/eslint-plugin": "^6.9.0",
    "@typescript-eslint/parser": "^6.9.0",
    "@vscode/test-electron": "^2.3.6",
    "eslint": "^8.52.0",
    "glob": "^13.0.3",
    "mocha": "^10.2.0",
    "typescript": "^5.2.2",
    "@inquirer/prompts": "^8.2.1"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "lru-cache": "^11.2.6"
  }
}
